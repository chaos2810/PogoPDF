{
  "name": "@pogopdf/engine",
  "version": "0.1.0",
  "type": "module",
  "main": "src/engine.ts",
  "scripts": {
    "build": "esbuild src/engine.ts --bundle --platform=node --target=node20 --outfile=dist/engine.cjs --external:@pogopdf/contracts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pogopdf/contracts": "*",
    "pdf-lib": "^1.17.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
